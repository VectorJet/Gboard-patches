<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- NexusLauncher Taskbar static RRO: shrink the 60px taskbar strip so the gap
         between Gboard and the pill closes. Real names verified via cmd overlay lookup:
         taskbar_stashed_size (24dp)=the Taskbar window height; missing names are skipped. -->
    <dimen name="taskbar_stashed_size">16dp</dimen>
    <dimen name="taskbar_stashed_handle_height">4dp</dimen>
    <dimen name="taskbar_hotseat_nav_spacing">0dp</dimen>
</resources>